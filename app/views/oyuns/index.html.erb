 <%= render partial: "layouts/shared/arka_taraf/navbar" %>
<p id="notice"><%= notice %></p>

<h1>Oyuns</h1>

<table>
  <thead>
    <tr>
      <th>Isim</th>
      <th>Aciklama</th>
      <th>Resim</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @oyuns.each do |oyun| %>
      <tr>
        <td><%= oyun.isim %></td>
        <td><%= oyun.aciklama %></td>
        <td><%= image_tag oyun.resim.url(:thumb) if oyun.resim? %> </td>
        <td><%= link_to 'Show', oyun %></td>
        <td><%= link_to 'Edit', edit_oyun_path(oyun) %></td>
        <td><%= link_to 'Destroy', oyun, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Oyun', new_oyun_path %>
